TDAT: public void concat ( big integer hi , int hi width in bits )  <NL>  { if ( assemble . arch . big endian )  <NL> number = number . shift left ( hi width in bits ) . or ( bit field . mask to width ( hi , hi width in bits ) ) ;  <NL> else number = hi . shift left ( width in bits ) . or ( bit field . mask to width ( number , width in bits ) ) ;  <NL> width in bits + = hi width in bits ;  <NL>  }  COM: <s> join two target bitstrings together </s>