TDAT: protected void set up ( ) throws exception {  <NL> super . set up ( ) ;  <NL>  <NL> connection conn = get connection ( ) ;  <NL> conn . set auto commit ( false ) ;  <NL> prepared statement insert = conn . prepare statement (  <NL>  " insert into " + table name + " values ( ? , ? , ? , ? , ? , ? , ? , ? , ? ) " ) ;  <NL> load data ( insert ) ;  <NL> insert . close ( ) ;  <NL> conn . close ( ) ;  <NL>  }  COM: <s> clean the default database using the default connection </s>