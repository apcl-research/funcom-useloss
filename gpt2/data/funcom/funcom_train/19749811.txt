TDAT: protected session factory lookup session factory ( ) {  <NL> if ( log . is debug enabled ( ) ) {  <NL> log . debug ( " using session factory ' " + get session factory bean name ( ) + " ' for startup listener " ) ;  <NL>  }  <NL> web application context wac = web application context utils . get required web application context ( servlet context ) ;  <NL> return ( session factory ) wac . get bean ( get session factory bean name ( ) ) ;  <NL>  }  COM: <s> looks up the session factory that this filter should use </s>