TDAT: public boolean has next ( ) {  <NL> if ( distance < start dist & & next distance > start dist )  <NL> return true ;  <NL>  <NL> if ( distance < end dist & & next distance > end dist )  <NL> return true ;  <NL> int x = is backwards ? last vert - current vert : current vert <NL>  - first vert ;  <NL> x = ( x + verts . length ) % verts . length ;  <NL> x = ( last vert - first vert + verts . length ) % verts . length - x ;  <NL>  <NL> return x > 0 ;  <NL>  }  COM: <s> check if there are still vertices to walk to </s>