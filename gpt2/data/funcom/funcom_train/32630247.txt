TDAT: private void dispose weak ( ) {  <NL> int tries = 0 ;  <NL> while ( f base cache . size ( ) > 0 & & tries < max _ dispose _ tries ) {  <NL> tries + + ;  <NL> get monitor ( ) . debug ( " waiting for notes disposal . ( " + f base cache . size ( ) + " objects in queue ) " ) ;  <NL> system . gc ( ) ;  <NL> recycle queue ( ) ;  <NL> sleep ( time _ wait _ for _ gc ) ;  <NL>  }  <NL>  }  COM: <s> disposes all objects that have only weak references </s>