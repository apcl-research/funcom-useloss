TDAT: public void set base64strings ( collection < base64string bean > i base64strings ) {  <NL> if ( i base64strings = = null || i base64strings . size ( ) ! = 1 ) {  <NL> throw new illegal state exception ( " the base64string bean collection should have one and only one entry for each binary array object " ) ;  <NL>  }  <NL> this . i base64strings = i base64strings ;  <NL> refresh base64string ( ) ;  <NL>  }  COM: <s> sets the collection of base 64 chunks held by this bean </s>