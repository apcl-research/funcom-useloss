TDAT: public void move ( span s ) {  <NL>  / / assume span valid - - just mark dirty and repaint services <NL> if ( is set ( ) ) mark dirty ( ) ; / / old position <NL> moveq ( s ) ;  <NL> if ( is set ( ) ) mark dirty ( ) ; / / new position <NL> browser br = get browser ( ) ; if ( br ! = null ) br . repaint ( 250 ) ;  <NL>  }  COM: <s> span endpoints assumed valid just moveq reformatting and repainting </s>