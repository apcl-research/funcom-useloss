TDAT: public type reference get innermost element type ( ) {  <NL> type reference elem = get array element type ( ) ;  <NL> if ( elem . is array type ( ) ) {  <NL>  / / note : we must recur instead of attempting to parse <NL>  / / the array descriptor for [ ' s so we correctly handle <NL>  / / [ address array and similar evil vm magic <NL> return elem . get innermost element type ( ) ;  <NL>  } else {  <NL> return elem ;  <NL>  }  <NL>  }  COM: <s> return the innermost element type reference for an array </s>