TDAT: private int get first row on page y ( int page y ) {  <NL> int rows per page = page area . height / ( int ) ( row height * zoom ) ;  <NL> int first row = rows per page * page y - 1 ; / / subract 1 for header row <NL> first row = first row < 0 ? 0 : first row ;  <NL> first row = first row > get row count ( ) ? get row count ( ) : first row ;  <NL> return first row ;  <NL>  }  COM: <s> calculates the first row number to appear on </s>