TDAT: private void get ( byte [ ] buf , int len ) {  <NL> if ( len > end - offset ) {  <NL> log . get log instance ( ) . fatal ( " buffer . get ( ) : trying to get more bytes " + len + " than in buffer " + ( end - offset ) ) ;  <NL>  }  <NL> system . arraycopy ( this . buf , offset , buf , 0 , len ) ;  <NL> offset + = len ;  <NL>  }  COM: <s> gets data from the beginning of the buffer </s>