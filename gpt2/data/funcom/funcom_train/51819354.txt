TDAT: public string get group variable name ( dj group group ) {  <NL> string column to group by property = null ;  <NL>  / / if ( group ! = null )  <NL> column to group by property = group . get column to group by ( ) . get column property ( ) . get property ( ) ;  <NL>  / / else <NL>  / / column to group by property = " global " ;  <NL> return " variable - " + column to group by property + " _ " + get percentage column ( ) . get column property ( ) . get property ( ) + " _ percentage " ;  <NL>  }  COM: <s> the group which the variable will be inside mostly for reset </s>