TDAT: public int digit length ( ) {  <NL> int bit length = this . bit length ( ) ;  <NL> int min = ( int ) ( bit length * bits _ to _ digits ) + 1 ;  <NL> int max = ( int ) ( ( bit length + 1 ) * bits _ to _ digits ) + 1 ;  <NL> if ( min = = max )  <NL> return min ;  <NL> return ( large integer . one . times10pow ( min + 1 ) . is larger than ( this ) ) ? min <NL>  : min + 1 ;  <NL>  }  COM: <s> returns the minimal number of decimal digits necessary to represent </s>