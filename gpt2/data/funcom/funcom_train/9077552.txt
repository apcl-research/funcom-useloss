TDAT: public int cell to offset ( int row , int col ) {  <NL>  / / check row and column individually to prevent them being invalid <NL>  / / values but still pointing to a valid offset in the buffer .  <NL> if ( row < 0 || row > = get row count ( ) || <NL> col < 0 || col > 15 ) { / / don ' t include last column ( ascii dump )  <NL> return - 1 ;  <NL>  }  <NL> int offs = row * 16 + col ;  <NL> return ( offs > = 0 & & offs < model . get byte count ( ) ) ? offs : - 1 ;  <NL>  }  COM: <s> returns the offset into the bytes being edited represented at the </s>