TDAT: public int get peer node routing backoff reason size ( string peer node routing backoff reason ) {  <NL> hash set < peer node > reason set = null ;  <NL> synchronized ( peer node routing backoff reasons ) {  <NL> if ( peer node routing backoff reasons . contains key ( peer node routing backoff reason ) ) {  <NL> reason set = peer node routing backoff reasons . get ( peer node routing backoff reason ) ;  <NL> return reason set . size ( ) ;  <NL>  } else <NL> return 0 ;  <NL>  }  <NL>  }  COM: <s> how many peer nodes have a particular routing backoff reason </s>