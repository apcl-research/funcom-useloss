TDAT: public void deregister ( bean persist controller controller ) {  <NL>  / / volatile read . . .  <NL> bean persist controller c = persist controller ;  <NL> if ( c = = null ) {  <NL>  / / nothing to deregister <NL>  } else {  <NL> if ( c instanceof chained bean persist controller ) {  <NL>  / / remove it from the existing chain <NL> persist controller = ( ( chained bean persist controller ) c ) . deregister ( controller ) ;  <NL>  } else if ( c . equals ( controller ) ) {  <NL> persist controller = null ;  <NL>  }  <NL>  }  <NL>  }  COM: <s> de register the bean persist controller </s>