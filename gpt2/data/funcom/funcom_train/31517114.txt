TDAT: private string get i pv4 ( byte [ ] buffer , int index ) {  <NL> string ip addr = ( int ) ( buffer [ index ] & 0xff ) + " . " + ( int ) ( buffer [ index + 1 ] & 0xff ) + " . " +  <NL>  ( int ) ( buffer [ index + 2 ] & 0xff ) + " . " + ( int ) ( buffer [ index + 3 ] & 0xff ) ;  <NL> return ( ip addr ) ;  <NL>  }  COM: <s> build an ipv4 address string from the buffered data </s>