TDAT: public void zoom to ( rectangle rect ) {  <NL> dimension available = get viewport ( ) . get client area ( ) . get size ( ) ;  <NL> dimension desired = rect . get size ( ) ;  <NL>  <NL> double scale x = available . width * get zoom ( ) / desired . width ;  <NL> double scale y = available . height * get zoom ( ) / desired . height ;  <NL>  <NL> double zoom = math . min ( get max zoom ( ) , math . max ( get min zoom ( ) , math . min ( scale x , scale y ) ) ) ;  <NL> zoom to ( zoom , rect . get center ( ) ) ;  <NL>  }  COM: <s> allows implementors to zoom into or out to a rectangular area </s>