TDAT: public void set status bar text ( string s , boolean lock , boolean force ) {  <NL> if ( status bar = = null || ( lock status text & & ! force ) ) return ;  <NL>  <NL>  / / if locking set the lock <NL> if ( lock )  <NL> lock status text = true ;  <NL>  / / else not locking and forcing unset the lock <NL> else if ( force )  <NL> lock status text = false ;  <NL>  <NL>  <NL> last status text = current status text ;  <NL> current status text = s ;  <NL> status bar . set text ( s ) ;  <NL>  }  COM: <s> set the status bars text and possibly lock it </s>