TDAT: protected session factory lookup session factory ( ) {  <NL>  / / if ( logger . is debug enabled ( ) ) {  <NL>  / / logger . debug ( " using session factory ' " + get session factory bean name ( ) + " ' for open session in view filter " ) ;  <NL>  / / }  <NL>  / / return ( session factory ) application context holder . get bean ( session factory bean name ) ;  <NL> if ( session factory = = null )  <NL> throw new illegal state exception ( " ' session factory ' property must be not null " ) ;  <NL> return session factory ;  <NL>  }  COM: <s> look up the session factory that this filter should use </s>