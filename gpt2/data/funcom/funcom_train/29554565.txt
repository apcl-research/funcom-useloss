TDAT: public void delete all ( ) {  <NL> hibernate template template = get hibernate template ( ) ;  <NL>  / / todo : do we really need to specify the table name ? ? can we get this info from the class ? ?  <NL> list < ? > entities = template . find ( " from " + table name ) ;  <NL> template . delete all ( entities ) ;  <NL> template . flush ( ) ;  <NL>  }  COM: <s> deletes all code iproduct code from the database </s>