TDAT: public boolean is all zoomed out ( ) {  <NL>  / / this one is pretty good , except that if you are all zoomed out , then make the frame smaller along the map axis , it starts returning false before the viewport layout mgr starts resizing things , so the scrollable j panel says not to track the map axis dimension . . .  <NL>  / / return ( map panel . get pixel size along map axis ( ) < = available map axis pixels ( ) ) ;  <NL> return all zoomed out ;  <NL>  }  COM: <s> indicates whether the map display exactly fills the display </s>