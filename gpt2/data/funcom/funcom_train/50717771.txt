TDAT: public dimension minimum layout size ( container parent ) {  <NL>  <NL> insets insets = parent . get insets ( ) ;  <NL> get grid sizes ( parent , true ) ;  <NL> return new dimension ( insets . left + insets . right + sum ( col _ widths )  <NL>  + ( col _ widths . length + 1 ) * hgap ,  <NL> insets . top + insets . bottom + sum ( row _ heights )  <NL>  + ( row _ heights . length + 1 ) * vgap ) ;  <NL>  }  COM: <s> returns the minimum dimensions needed to layout the components </s>