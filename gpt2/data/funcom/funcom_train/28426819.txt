TDAT: public void update ( byte [ ] buffer , int offset , int length ) {  <NL>  / / make sure we don ' t exceed buffer ' s allocated size / length <NL> if ( ( length + offset ) > buffer . length )  <NL> length = buffer . length - offset ;  <NL> while ( length - - > 0 )  <NL> update ( buffer [ offset + + ] ) ;  <NL>  }  COM: <s> processes a buffer of bytes into the crc accumulator </s>