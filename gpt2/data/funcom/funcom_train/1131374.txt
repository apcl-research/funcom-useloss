TDAT: private void resize ( ) {  <NL> dimension to zoom ;  <NL> if ( zoom > = 1 )  <NL>  / / zoom in <NL> to zoom = new dimension ( real size . width * ( zoom + 1 ) , real size . height * ( zoom + 1 ) ) ;  <NL> else if ( zoom < = - 1 )  <NL>  / / zoom out <NL> to zoom = new dimension ( real size . width / ( 1 - zoom ) , real size . height / ( 1 - zoom ) ) ;  <NL> else <NL> to zoom = real size ;  <NL>  / / resize component <NL> set size ( to zoom ) ;  <NL> set preferred size ( to zoom ) ;  <NL> set minimum size ( to zoom ) ;  <NL>  }  COM: <s> resizes the component to match the zoom </s>