TDAT: public void set conflict resolution strategy ( conflict resolution strategy strat ) {  <NL> if ( strat ! = null ) {  <NL> strategy = strat ;  <NL>  / / we need to recompute the activation list <NL> queue < activation > old list = activations ;  <NL> int old list size = old list . size ( ) ;  <NL> int new initial capacity = old list size > 0 ? old list size <NL>  : initial _ capacity ;  <NL> activations = new priority queue < activation > ( new initial capacity ,  <NL> strategy ) ;  <NL> activations . add all ( old list ) ;  <NL>  } else <NL> throw new null pointer exception ( ) ;  <NL>  }  COM: <s> sets the conflict resolution strategy </s>