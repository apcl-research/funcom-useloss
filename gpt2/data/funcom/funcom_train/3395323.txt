TDAT: public package doc impl lookup package ( string name ) {  <NL>  / / # # # jing alleges that class check is needed <NL>  / / # # # to avoid a compiler bug . most likely <NL>  / / # # # instead a dummy created for error recovery .  <NL>  / / # # # should investigate this .  <NL> package symbol p = syms . packages . get ( names . from string ( name ) ) ;  <NL> class symbol c = get class symbol ( name ) ;  <NL> if ( p ! = null & & c = = null ) {  <NL> return get package doc ( p ) ;  <NL>  } else {  <NL> return null ;  <NL>  }  <NL>  }  COM: <s> look up package doc by qualified name </s>