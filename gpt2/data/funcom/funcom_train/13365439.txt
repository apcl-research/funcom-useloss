TDAT: public boolean has children ( object a parent ) {  <NL> if ( a parent = = this ) / / the datasource itself is the root of all objects queried by the framework .  <NL> return true ; / / since we fill the list it hopefully will never get empty .  <NL> if ( a parent instanceof office )  <NL> return ( ( office ) a parent ) . has asset ( ) ;  <NL> return false ; / / we can return false since an asset has no children .  <NL>  }  COM: <s> method called by the framework to query whether the passed parent </s>