TDAT: protected void fire collection change event ( ) {  <NL> collection change event event = new collection change event ( this ) ;  <NL>  <NL> object [ ] listeners = listener list . get listener list ( ) ;  <NL>  / / each listener occupies two elements - the first is the listener class <NL>  / / and the second is the listener instance <NL> for ( int i = 0 ; i < listeners . length ; i + = 2 ) {  <NL> if ( listeners [ i ] = = collection change listener . class ) {  <NL>  ( ( collection change listener ) listeners [ i + 1 ] ) . collection change ( event ) ;  <NL>  }  <NL>  }  <NL>  }  COM: <s> fires a collection change event </s>