TDAT: private string get prefix key ( artifact artifact ) {  <NL>  / / use the hashcode as a uuid for the artifact ( fingers crossed )  <NL> int hash code = artifact . get id ( ) . hash code ( ) ;  <NL>  / / use just some visual cue <NL> return " artifact : " + artifact . get name ( ) + " # " + artifact . get type ( ) + " # "  <NL>  + artifact . get ext ( ) + " # " + hash code ;  <NL>  }  COM: <s> creates the unique prefix key that will reference the artifact within the properties </s>