TDAT: public void roll ( double distance , int speed , boolean immediate return ) {  <NL> double avg radius = ( left _ radius + right _ radius ) / 2 . 0 ;  <NL> left motor . set speed ( ( int ) ( avg radius * speed / left _ radius ) ) ;  <NL> right motor . set speed ( ( int ) ( avg radius * speed / right _ radius ) ) ;  <NL> left motor . rotate ( ( int ) ( distance / ( 2 * math . pi * left _ radius ) * 360 ) , true ) ;  <NL> right motor . rotate ( ( int ) ( distance / ( 2 * math . pi * right _ radius ) * 360 ) , immediate return ) ;  <NL>  }  COM: <s> used for calibration of mechanical constants </s>