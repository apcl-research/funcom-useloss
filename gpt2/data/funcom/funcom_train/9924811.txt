TDAT: public void export global ( )  <NL>  { symbol sym = global symbols . get ( name ) ;  <NL> global symbols . put ( name , this ) ;  <NL> if ( ! assemble . first pass )  <NL> if ( sym ! = null & & sym ! = this ) / / defer error to get msg from each instance <NL> assemble . throw err ( " global symbol " + name + " exported by another file " ) ;  <NL> else if ( number = = null )  <NL> assemble . throw err ( " exported symbol " + name + " not defined " ) ;  <NL>  }  COM: <s> define a global symbol so it can be referenced in other namespaces </s>