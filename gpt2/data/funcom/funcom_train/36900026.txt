TDAT: private string type2string ( class < ? > type ) {  <NL> string name = type . get canonical name ( ) ;  <NL>  / / todo rewrite it in the following manner : if type is connected anyhow with the old package <NL> if ( needs conversion ( type ) )  <NL> name = code generator settings . editor _ beans _ package + ' . ' +  <NL> make new class name ( type ) ;  <NL> return change bin package ( name ) ;  <NL>  }  COM: <s> converts type to its name to use in code </s>