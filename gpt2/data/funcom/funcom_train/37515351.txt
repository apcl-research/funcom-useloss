TDAT: private void process pop ( ) {  <NL>  / / if we ' re popping from a discarded language then keep the <NL>  / / current nested language buffer for indexing . otherwise add <NL>  / / future indices to the buffer for the language that we are <NL>  / / popping from .  <NL> if ( ! discarding ) {  <NL> nested language buffer = active stream buffer . buffer ;  <NL>  }  <NL> set active ( ( string ) key stack . pop ( ) ,  <NL>  ( stream buffer pair ) stream buffer stack . pop ( ) ) ;  <NL>  }  COM: <s> pops a stream from the stack of input streams </s>