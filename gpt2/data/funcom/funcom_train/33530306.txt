TDAT: public void set horizontal zoom relative ( float horizontal zoom ) {  <NL> this . horizontal zoom relative = horizontal zoom ;  <NL>  <NL> float samples = sample = = null ? 1000 : sample . get float sample buffer ( ) . get sample count ( ) ;  <NL> float viewport = ( ( j viewport ) get parent ( ) ) . get width ( ) ;  <NL>  <NL> float max zoom = 1 . 0f ;  <NL> float min zoom = viewport / samples ;  <NL>  <NL>  / / samples / viewport * ho zoom <NL> float zoom = min zoom + horizontal zoom * ( max zoom - min zoom ) ;  <NL> set horizontal zoom ( zoom ) ;  <NL>  <NL>  }  COM: <s> set the relative horizontal zoom </s>