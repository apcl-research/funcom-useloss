TDAT: public void action performed ( action event ae ) {  <NL> j menu item mi = ( j menu item ) ae . get source ( ) ;  <NL> string name = mi . get name ( ) ;  <NL> object o = plugintable . get ( name ) ;  <NL> if ( o instanceof plug let if ) {  <NL> plug let if plugin = ( plug let if ) plugintable . get ( name ) ;  <NL>  / / plugin . execute ( app . get ggb api ( ) ) ;  <NL> plugin . execute ( ) ;  <NL>  } else {  <NL> application <NL>  . debug ( " no plug let if called " + name + " in plugintable ! " ) ;  <NL>  } / / if - else <NL>  } / / action performed ( action event )  COM: <s> implementing action listener for menu items </s>