TDAT: private boolean is match ( final byte [ ] pattern , final int offset , final int start ) {  <NL>  / / if the offset equals pattern length then we have successfully matched every byte <NL>  / / if the byte in pattern matches the corresponding byte in _ data then check the next byte and so forth <NL> return offset = = pattern . length ? true : ( ( pattern [ offset ] = = _ data [ start ] ) ? is match ( pattern , offset + 1 , start + 1 ) : false ) ;  <NL>  }  COM: <s> determine if the pattern matches the specified range of bytes in this blob </s>