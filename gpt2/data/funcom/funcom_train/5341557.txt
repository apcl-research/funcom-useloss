TDAT: public void do refresh ( ) {  <NL> boolean inactive present =  <NL>  ( ( boolean ) data _ model . refresh ( ) ) . boolean value ( ) ;  <NL> set button enabled ( upload buttons . clear _ button , inactive present ) ;  <NL>  <NL> main _ panel . set title ( upload _ title + " ( " +  <NL> router service . get upload manager ( ) . uploads in progress ( ) + " " +  <NL> active + " , " +  <NL> router service . get upload manager ( ) . get num queued uploads ( ) + " " +  <NL> queued + " ) " ) ;  <NL>  }  COM: <s> override the default refresh so we can set the clear button </s>