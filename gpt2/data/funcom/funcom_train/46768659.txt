TDAT: public boolean is on screen ( int left , int top , int right , int bottom ) {  <NL> float screen x = get screen x ( ) ;  <NL> float screen y = get screen y ( ) ;  <NL> viewport viewport = layer . get viewport ( ) ;  <NL> return screen x + get width ( ) + left > 0 & & screen y + get height ( ) + top > 0 & &  <NL> screen x < viewport . get clip ( ) . get width ( ) + right & & screen y < viewport . get clip ( ) . get height ( ) + bottom ;  <NL>  }  COM: <s> returns whether this actor is within the cameras view </s>