TDAT: public int size ( ) {  <NL> int size = 0 ;  <NL> size + = writable utils . get v int size ( cumulative cpu usage ) ; / / long # 1 <NL> size + = writable utils . get v int size ( virtual memory usage ) ; / / long # 2 <NL> size + = writable utils . get v int size ( physical memory usage ) ; / / long # 3 <NL> size + = writable utils . get v int size ( heap usage ) ; / / long # 4 <NL> return size ;  <NL>  }  COM: <s> returns the size of the serialized data </s>