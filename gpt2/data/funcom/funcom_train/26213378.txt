TDAT: public long skip ( long n ) throws io exception {  <NL>  / / make sure we don ' t skip more bytes than are <NL>  / / still available <NL> long length = math . min ( n , content length - pos ) ;  <NL>  / / skip and keep track of the bytes actually skipped <NL> length = wrapped stream . skip ( length ) ;  <NL>  / / only add the skipped bytes to the current position <NL>  / / if bytes were actually skipped <NL> if ( length > 0 ) {  <NL> pos + = length ;  <NL>  }  <NL> return length ;  <NL>  }  COM: <s> skips and discards a number of bytes from the input stream </s>