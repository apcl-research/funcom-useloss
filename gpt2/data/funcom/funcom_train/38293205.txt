TDAT: private void encode and append ( string val ) {  <NL>  / / if the slot name is a string of words then quote it . if it is a meta ( i . e . starts with " ? ? " ) do not quote it .  <NL> string out = ( ( simple sl tokenizer . is a word ( val ) || ( meta allowed & & val . starts with ( " ? ? " ) ) ) ? val : simple sl tokenizer . quote string ( val ) ) ;  <NL> buffer . append ( out ) ;  <NL>  }  COM: <s> encode a string taking care of quoting separated words and </s>