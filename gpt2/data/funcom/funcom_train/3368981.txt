TDAT: private void cache sort keys ( list < ? extends sort key > keys ) {  <NL> int key size = keys . size ( ) ;  <NL> sort comparators = new comparator [ key size ] ;  <NL> for ( int i = 0 ; i < key size ; i + + ) {  <NL> sort comparators [ i ] = get comparator0 ( keys . get ( i ) . get column ( ) ) ;  <NL>  }  <NL> cached sort keys = keys . to array ( new sort key [ key size ] ) ;  <NL>  }  COM: <s> caches the sort keys before a sort </s>