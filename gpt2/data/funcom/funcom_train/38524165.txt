TDAT: public feed parse ( ) {  <NL>  <NL>  / / create new xtom parser <NL> parser p = new parser ( xml ) ;  <NL>  / / parse the xml and create an xml tree .  <NL> xml tree tree = p . parse ( ) ;  <NL>  <NL>  / / now check if the root element is < rss >  <NL> element root = tree . get root element ( ) ;  <NL> if ( root . get name ( ) ! = " rss " ) throw new runtime exception ( " the root element was not < rss > . can ' t parse this rss feed " ) ;  <NL>  <NL>  / / now we are ready to parse the rss feed .  <NL> return parse feed ( root ) ;  <NL>  }  COM: <s> parses the rss feed and returns the rss feed document </s>