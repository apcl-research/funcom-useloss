TDAT: public string get recording info ( ) {  <NL> string return _ string = audio methods . get audio format data ( audio _ format ) ;  <NL>  <NL> string number _ samples = get number samples per channel ( ) + " samples\n " ;  <NL> string duration = get duration ( ) + " seconds\n " ;  <NL> string max _ sample _ value = get maximum amplitude ( ) + " \n " ;  <NL>  <NL> return _ string + = new string ( " samples per channel : " + number _ samples ) ;  <NL> return _ string + = new string ( " duration : " + duration ) ;  <NL> return _ string + = new string ( " max signal amplitude : " + max _ sample _ value ) ;  <NL> return return _ string ;  <NL>  }  COM: <s> returns a formatted description of the audio format of the samples as well </s>