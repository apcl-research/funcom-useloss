TDAT: public void parse contents ( parser parser ) {  <NL>  / / indicate to the top - level stylesheet that all templates must be <NL>  / / compiled into separate methods .  <NL> stylesheet stylesheet = get stylesheet ( ) ;  <NL> stylesheet . set template inlining ( false ) ;  <NL>  <NL>  / / get the mode we are currently in ( might not be any )  <NL> template template = get template ( ) ;  <NL>  _ mode name = template . get mode name ( ) ;  <NL>  _ precedence = template . get import precedence ( ) ;  <NL>  <NL> parse children ( parser ) ; / / with - params <NL>  }  COM: <s> parse the attributes and contents of an xsl apply imports element </s>