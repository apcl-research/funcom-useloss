TDAT: public boolean do init ( config config ) {  <NL> reload _ time = config . get property ( " reloadtime " ) ;  <NL> news _ file = config . get property ( " newsfile " ) ;  <NL> server = config . get property ( " server " ) ;  <NL> img _ server = config . get property ( " img _ server " ) ;  <NL> prop _ file = config . get property ( " propfile " ) ;  <NL>  <NL> return true ;  <NL>  }  COM: <s> do some initialization for this application e </s>