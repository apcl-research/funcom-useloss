TDAT: protected boolean should wipe unused cache ( ) {  <NL>  / / here is how it works : if the cache havent been accessed in the last <NL>  / / " configuration . max _ keep _ unused _ partial _ cache " garbage collection , then we <NL>  / / will wipe its contecnt since partial clean costs too much <NL>  <NL> if ( access _ last _ gc = = num _ access ) cache _ not _ used _ count + + ;  <NL> else cache _ not _ used _ count = 0 ;  <NL>  <NL> access _ last _ gc = num _ access ;  <NL>  <NL> return ( cache _ not _ used _ count > configuration . max _ keep _ unused _ partial _ cache ) ;  <NL>  }  COM: <s> if this function returns true we should wipe the cache entirely instead </s>