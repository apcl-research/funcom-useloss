TDAT: public void snapshot ( ) {  <NL> snapshot width = width ;  <NL> snapshot height = height ;  <NL> if ( snapshot pixels = = null || ( snapshot pixels ! = null & & snapshot pixels . length ! = pixels . length ) )  <NL> snapshot pixels = new byte [ width * height ] ;  <NL> system . arraycopy ( pixels , 0 , snapshot pixels , 0 , width * height ) ;  <NL> new snapshot = true ;  <NL>  / / double sum = 0 ;  <NL>  / / for ( int i = 0 ; i < width * height ; i + + )  <NL>  / / sum + = pixels [ i ] & 0xff ;  <NL>  / / ij . ij . write ( " snapshot : " + ( sum / ( width * height ) ) ) ;  <NL>  }  COM: <s> make a snapshot of the current image </s>