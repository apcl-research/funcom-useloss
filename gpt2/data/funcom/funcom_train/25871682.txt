TDAT: protected session factory lookup session factory ( ) {  <NL> if ( logger . is debug enabled ( ) ) {  <NL> logger . debug ( " using session factory ' " + get session factory bean name ( ) + " ' for open session in view filter " ) ;  <NL>  }  <NL> application context ctx = application context utils . get web context ( get servlet context ( ) ) ;  <NL> return ctx . get bean ( get session factory bean name ( ) , session factory . class ) ;  <NL>  }  COM: <s> look up the session factory that this filter should use </s>