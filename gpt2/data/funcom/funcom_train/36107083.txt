TDAT: public data get module data ( string module key ) {  <NL> return load module data ( module key ) ;  <NL>  / / caching the whole object is wrong because the module ' s metadata can be changed ( e . g . manually ) .  <NL>  / / only top - level fields ( id , module type , path and system name ) could be cached here and checked in agent .  <NL>  / / data module = get modules ( ) . get ( module key ) ;  <NL>  / / if ( module = = null ) {  <NL>  / / module = load module data ( module key ) ;  <NL>  / / get modules ( ) . put ( module key , module ) ;  <NL>  / / }  <NL>  / / return module ;  <NL>  }  COM: <s> this implementation assumes that </s>