TDAT: public boolean get ( int index ) {  <NL> int i = index > > 6 ; / / div 64 <NL>  / / signed shift will keep a negative index and force an <NL>  / / array - index - out - of - bounds - exception , removing the need for an explicit check .  <NL> if ( i > = bits . length ) return false ;  <NL>  <NL> int bit = index & 0x3f ; / / mod 64 <NL> long bitmask = 1l < < bit ;  <NL> return ( bits [ i ] & bitmask ) ! = 0 ;  <NL>  }  COM: <s> returns true or false for the specified bit index </s>