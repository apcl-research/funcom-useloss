TDAT: public void update cursor ( ) {  <NL>  / / test for illegal types <NL>  _ cursor type = ( ( _ cursor type < cursor . default _ cursor ) || ( _ cursor type > cursor . move _ cursor ) ) ? cursor . default _ cursor <NL>  : _ cursor type ;  <NL>  / / if different cursor is requested , set new cursor <NL> if ( _ component . get cursor ( ) . get type ( ) ! = _ cursor type ) {  <NL>  _ component . set cursor ( new cursor ( _ cursor type ) ) ;  <NL>  }  <NL>  / / reset cursor - request <NL>  _ cursor type = cursor . default _ cursor ;  <NL>  }  COM: <s> sets the requested cursor and resets the cursor request </s>