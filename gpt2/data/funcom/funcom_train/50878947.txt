TDAT: private envelope get layer envelope ( ) throws exception {  <NL>  <NL>  / / loop through all the layers getting the maximum and minimum bounds <NL> envelope buffer temp envelope buffer = new envelope buffer ( ) ;  <NL> for ( int i = 0 ; i < my layers . length ; i + + ) {  <NL> temp envelope buffer . expand to include ( my layers [ i ] . get envelope ( ) ) ;  <NL>  }  <NL> return temp envelope buffer . get envelope ( ) ;  <NL>  }  COM: <s> get the default bounds from all of the layers </s>