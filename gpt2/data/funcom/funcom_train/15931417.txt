TDAT: protected void on start ( ) {  <NL>  / / allocate while starting to improve chances of thread - local <NL>  / / isolation <NL> queue = new fork join task < ? > [ initial _ queue _ capacity ] ;  <NL>  / / initial value of seed need not be especially random but <NL>  / / should differ across workers and must be nonzero <NL> int p = pool index + 1 ;  <NL> seed = p + ( p < < 8 ) + ( p < < 16 ) + ( p < < 24 ) ; / / spread bits <NL>  }  COM: <s> initializes internal state after construction but before </s>