TDAT: protected boolean do is special request ( join point request r1 ) {  <NL>  / / we get here if and only if ( spec1 . may filter | spec2 . may filter ) is fits the r1 . mask <NL>  / / in case it 0 , then the abstract crosscut specializer has already <NL>  / / said ' yes ' .  <NL>  / / the following conditions reads as follows :  <NL>  / / ( if spec1 may filter r1 and it actually filters it ) or ( spec3 may filter . . )  <NL> return <NL>  ( spec1 . is special request ( r1 ) )  <NL> || <NL>  ( spec2 . is special request ( r1 ) ) ;  <NL>  }  COM: <s> return true if either of the specializers passed in the crosscut </s>