TDAT: public void negate num ( ) {  <NL> int index = return first num index ( ) ;  <NL> if ( index ! = - 1 & & display . char at ( index ) ! = 0 ) { / / there is a num and its not 0 <NL> if ( index = = 0 ) / / if its first <NL> display . insert ( index , ' - ' ) ;  <NL> else if ( display . char at ( index - 1 ) = = ' - ' ) / / if its already negate <NL> display . delete char at ( index - 1 ) ;  <NL> else / / if its just a num <NL> display . insert ( index , ' - ' ) ;  <NL>  }  <NL>  }  COM: <s> negates the first actual number in the buffer </s>