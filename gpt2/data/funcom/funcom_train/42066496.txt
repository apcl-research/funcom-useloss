TDAT: public void wizard panel removed ( wizard model event event ) {  <NL> wizard panel panel = event . get panel ( ) ;  <NL> component component = panel . get component ( ) ;  <NL>  <NL>  / / if the panel being removed was the current panel , then we have to <NL>  / / punt by resetting . unfortunately , we can ' t ask the model if this is <NL>  / / the current panel because the model has already removed it .  <NL>  / / todo find out if component is the current panel <NL>  <NL> card panel . remove ( component ) ;  <NL>  }  COM: <s> indicates that a wizard panel was remove from the model </s>