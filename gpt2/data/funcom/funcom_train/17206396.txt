TDAT: protected void set prev ( int unit , int prev ) {  <NL> if ( vm . verify _ assertions ) vm . assertions . _ assert ( ( prev > = - heads ) & & ( prev < = max _ units ) ) ;  <NL> int old value = get lo entry ( unit ) ;  <NL> int new value = ( old value & ~ prev _ mask ) | ( prev & prev _ mask ) ;  <NL> set lo entry ( unit , new value ) ;  <NL>  }  COM: <s> set the previous lump in the doubly linked free list </s>