TDAT: public dimension minimum layout size ( container target ) {  <NL>  <NL> insets insets = target . get insets ( ) ;  <NL> dimension tab = get minimum tab size ( ) ;  <NL> int h = tab . height *  <NL>  ( tabs . size ( ) + 1 ) + ( tabs . size ( ) * hgap ) ;  <NL> return new dimension (  <NL> tab . width + insets . left + insets . right + ( hgap * 2 ) ,  <NL> h + insets . top + insets . bottom ) ;  <NL>  }  COM: <s> returns the minimum dimensions needed to layout the </s>