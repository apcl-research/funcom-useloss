TDAT: protected void compute fields ( ) {  <NL> compute fields impl ( ) ;  <NL>  <NL>  / / careful here : we are manually setting the time stamps [ ]  <NL>  / / flags to internally _ set , so we must be sure that the <NL>  / / compute fields impl method actually does set all the fields .  <NL> for ( int i = 0 ; i < field _ count ; + + i ) {  <NL> stamp [ i ] = internally _ set ;  <NL> is set [ i ] = true ;  <NL>  }  <NL>  }  COM: <s> converts utc as milliseconds to time field values </s>