TDAT: public void init ( ) {  <NL> roots = get roots ( ) ;  <NL>  / / org . mmbase start ( ) ;  <NL> sessions = ( sessions interface ) get module ( " session " ) ;  <NL> scancache = ( scancache interface ) get module ( " scancache " ) ;  <NL> counter = ( counter interface ) get module ( " counter " ) ;  <NL> mmbase = ( mm base ) get module ( " mmbaseroot " ) ;  <NL> transactionhandler = ( transaction handler interface ) get module ( " transactionhandler " ) ;  <NL>  / / org . mmbase stats = ( statistics interface ) get module ( " stats " ) ;  <NL> rnd = new random plus ( ) ;  <NL> crcseed = rnd . next int ( ) ;  <NL>  }  COM: <s> init the servscan this is needed because it was created using </s>