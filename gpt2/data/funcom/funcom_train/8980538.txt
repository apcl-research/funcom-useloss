TDAT: public ecef coordinates ( double latitude , double longitude , double altitude ) {  <NL>  / / convert to radion <NL> latitude = latitude * pi / 180 . 0 ;  <NL> longitude = longitude * pi / 180 . 0 ;  <NL>  <NL> double h = sqrt ( 1 - e2square * sin ( latitude ) * sin ( latitude ) ) ;  <NL> double a hplus h = a / h + altitude ;  <NL>  <NL> set x ( a hplus h * cos ( latitude ) * cos ( longitude ) ) ;  <NL> set y ( a hplus h * cos ( latitude ) * sin ( longitude ) ) ;  <NL> set z ( ( ( a * ( 1 - e2square ) ) / h + altitude ) * sin ( latitude ) ) ;  <NL>  }  COM: <s> creates new ecef coordinates from lla </s>