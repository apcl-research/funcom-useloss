TDAT: public void init ( ) {  <NL> if ( olat context . is j unit test ( ) ) return ;  <NL>  / / load upgrades using spring framework <NL> xml bean factory upgrade factory = new xml bean factory ( new file system resource ( webapp helper . get context root ( ) + " / web - inf / " + upgrades _ file ) ) ;  <NL> upgrades definitions upgrade definitions = ( upgrades definitions ) upgrade factory . get bean ( " olatupgrades " ) ;  <NL> upgrades = upgrade definitions . get upgrades ( ) ;  <NL>  / / load history of previous upgrades using xstream <NL> init upgrades histories ( ) ;  <NL>  }  COM: <s> initialize the upgrade manager get all upgrades from the configuration file and load </s>