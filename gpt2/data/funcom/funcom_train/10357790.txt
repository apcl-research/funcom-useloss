TDAT: public address token next token ( ) {  <NL> address token token = ( address token ) tokens . get ( current token + + ) ;  <NL>  / / we skip over white space tokens when operating in this mode , so <NL>  / / check the token and iterate until we get a non - white space .  <NL> while ( token . type = = whitespace ) {  <NL> token = ( address token ) tokens . get ( current token + + ) ;  <NL>  }  <NL> return token ;  <NL>  }  COM: <s> get the next token at the cursor position advancing the </s>