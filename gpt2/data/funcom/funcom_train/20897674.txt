TDAT: public test protein utils ( string name ) {  <NL> super ( name ) ;  <NL>  <NL>  / / initialize the scripts folder by reading the scripts directory from the <NL>  / / configuration properties file <NL> scripts dir = app config . get instance ( ) . get property ( " scriptsfolder " ) ;  <NL>  <NL> if ( system . get property ( " panda . test . props . dir " ) ! = null ) {  <NL> test props dir = system . get property ( " panda . test . props . dir " ) + " org / tigr / panda / dataextract / protein utils / " ;  <NL>  } / / end of if ( )  <NL>  }  COM: <s> creates a new code test protein utils code instance </s>