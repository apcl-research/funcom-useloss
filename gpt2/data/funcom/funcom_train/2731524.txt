TDAT: public enum term visit ( enum term enum term ) {  <NL> logger . debug ( " < enum term > " ) ;  <NL> logger . debug ( " < before > " + printer . visit ( enum term ) + " < / before > " ) ;  <NL> logger . debug ( " < after > " + printer . visit ( enum term ) + " < / after > " ) ;  <NL> logger . debug ( " < / enum term > " ) ;  <NL> return enum term ;  <NL>  }  COM: <s> makes substitutions on an enumeration term </s>