TDAT: public void set path to names ( collection < list < ? > > names ) throws io exception {  <NL> if ( ( names = = null ) || names . is empty ( ) ) {  <NL> path to names = null ;  <NL> path to general names = null ;  <NL>  } else {  <NL> set < list < ? > > temp names = clone and check names ( names ) ;  <NL> path to general names = parse names ( temp names ) ;  <NL>  / / ensure that we either set both of these or neither <NL> path to names = temp names ;  <NL>  }  <NL>  }  COM: <s> sets the path to names criterion </s>