TDAT: public int get float width ( int side ) {  <NL>  / / / / system . out . println ( " get float width " + side + " , fc . leftw = " + fc . leftw ) ;  <NL> if ( side = = node . left ) return fc . leftw ;  <NL> else if ( side = = node . right ) return fc . rightw ;  <NL> else if ( side = = node . both ) return fc . leftw + fc . rightw ;  <NL> else return - 1 ;  <NL>  / / return ( side = = node . left ? fc . leftw : fc . rightw ) ;  <NL>  }  COM: <s> returns total width of active left floats </s>