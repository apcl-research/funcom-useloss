TDAT: protected color compute background ( component renderer , component adapter adapter ) {  <NL>  / / if this . background is null , use adapter . target . get background ( ) ;  <NL>  / / never use renderer . get background ( ) as the seed in this decorator <NL>  / / class because renderer is too promiscuous !  <NL> color seed = background = = null ? adapter . target . get background ( ) : background ;  <NL> return adapter . is selected ( ) ? compute selected background ( seed ) : seed ;  <NL>  }  COM: <s> p computes a suitable background for the renderer component within the </s>