TDAT: public buffered image create image ( int width , int height ) {  <NL> if ( width > picsize || height > picsize )  <NL> return null ;  <NL> else <NL>  / / we used to reuse old buffered images for new entries . however ,  <NL>  / / due to the delayed processus of image writing , this could lead <NL>  / / to serious screen garbage is the cache was too small .  <NL>  / / so we try a different strategy , just in case .  <NL> return new buffered image ( picsize , picsize ,  <NL> buffered image . type _ int _ argb ) ;  <NL>  / / return elements [ pos ] . image ;  <NL>  }  COM: <s> creates an image of the required size can fail </s>