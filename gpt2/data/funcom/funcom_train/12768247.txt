TDAT: public void refresh routes viewer ( object element ) {  <NL>  / / get expanded id ' s so we can maintain the view state <NL> string [ ] expanded id list = get expanded routes list ( ) ;  <NL>  <NL> if ( element = = null ) {  <NL> routes tree viewer . refresh ( ) ;  <NL>  } else {  <NL> routes tree viewer . refresh ( element ) ;  <NL>  }  <NL>  <NL>  / / reselect the tags that were selected prior to the refresh <NL> routes tree viewer . set expanded elements ( tag sea plugin . get default ( ) . get route collection ( ) . get routes ( expanded id list ) ) ;  <NL>  }  COM: <s> refreshes just the given element </s>