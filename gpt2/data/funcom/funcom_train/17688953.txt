TDAT: public string add order by ( string query ) {  <NL>  / / no order by <NL> if ( sort criterion = = null )  <NL> return query ;  <NL> int pos = locate order by ( query ) ;  <NL>  / / order by one criteria <NL> if ( pos = = - 1 )  <NL> return query + " order by " + sort criterion + " " + get sql sort direction ( ) ;  <NL>  / / order by two criteria <NL> return query . substring ( 0 , pos ) + sort criterion + " " + get sql sort direction ( ) + " , " + query . substring ( pos ) ;  <NL>  }  COM: <s> adds the order by parameter to a query </s>