TDAT: public boolean is default selected ( ) {  <NL>  / / only check until one of the pages have changes . this is <NL>  / / faster <NL>  / / than checking all till the end <NL> boolean def ault = true ;  <NL> for ( iterator < option > it = option order . iterator ( ) ; it . has next ( ) & &  <NL> def ault ; ) {  <NL> def ault & = it . next ( ) . is default selected ( ) ;  <NL>  }  <NL> return def ault ;  <NL>  }  COM: <s> checks if the defaults are selected everywhere </s>