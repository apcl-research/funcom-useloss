TDAT: public record route header get record route header ( )  <NL>  { / / header h = get header ( sip headers . record _ route ) ;  <NL>  / / if ( h = = null ) return null ; else return new record route header ( h ) ;  <NL> multiple header mh = get record routes ( ) ;  <NL> if ( mh = = null ) return null ; return new record route header ( mh . get top ( ) ) ;  <NL>  }  COM: <s> gets the top record route header </s>