TDAT: protected i shell monitor get shell monitor ( iui context ui ) {  <NL>  / *  <NL>  * fixme : this is how we * want * to access the shell monitor but doing so breaks some <NL>  * test lifecycle assumptions .  <NL>  *  <NL>  * http : / / code . google . com / p / wt - commons / issues / detail ? id = 25 <NL>  * /  <NL>  / / i shell monitor sm = ( i shell monitor ) ui . get adapter ( i shell monitor . class ) ;  <NL>  / / return sm ;  <NL> return shell monitor . get instance ( ) ;  <NL>  }  COM: <s> get an instance of the shell monitor </s>