TDAT: public void trigger write ( int se ) {  <NL> if ( ! m image )  <NL> log . error ( " doesn ' t make sense to trigger a write if not image " ) ;  <NL>  / / mark so writes all blocks <NL> keep writing = true ;  <NL>  / / format up and send the read command <NL> int block = ( se - 1 ) * ( entrysize / 4 ) ;  <NL> send write ( block ) ;  <NL>  }  COM: <s> start the process of writing the values for an se </s>