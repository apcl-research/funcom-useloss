TDAT: private void init codec ( io session session ) throws exception {  <NL>  / / creates the decoder and stores it into the newly created session <NL> protocol decoder decoder = factory . get decoder ( session ) ;  <NL> session . set attribute ( decoder , decoder ) ;  <NL>  <NL>  / / creates the encoder and stores it into the newly created session <NL> protocol encoder encoder = factory . get encoder ( session ) ;  <NL> session . set attribute ( encoder , encoder ) ;  <NL>  }  COM: <s> initialize the encoder and the decoder storing them in the </s>