TDAT: protected void join ( class clazz ) throws core xml exception {  <NL> if ( max occur > 1 )  <NL> throw new core xml exception (  <NL>  " max occur is " + max occur <NL>  + " but must be 1 for collector " ) ;  <NL> set clazz ( clazz ) ;  <NL>  / / xxx won ' t work unless default names assigned <NL>  / / find methods ( clazz ) ;  <NL> ordering . join ( clazz ) ;  <NL>  }  COM: <s> join this connector and its immediate descendents to its parent </s>