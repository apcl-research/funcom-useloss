TDAT: private void create forwarder ( socket inbound socket ) {  <NL> string base thread name = " prt " + integer . to string ( server port ) + " c " +  <NL> long . to string ( conn count + + ) ;  <NL> socket forwarder socket forwarder = new socket forwarder ( this ,  <NL> inbound socket , mapping , base thread name ) ;  <NL> synchronized ( forwarders ) {  <NL> forwarders . add ( socket forwarder ) ;  <NL>  }  <NL>  ( new thread ( socket forwarder , base thread name ) ) . start ( ) ;  <NL>  }  COM: <s> creates an outbound socket for the specified inbound socket according to </s>