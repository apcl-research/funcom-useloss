TDAT: private void update model from parent obj ( ) {  <NL> fill row with values ( parent obj . get kodper ( ) . split ( split _ string ) , kod _ per _ row ) ;  <NL> fill row with values ( parent obj . get kodeka ( ) . split ( split _ string ) , kod _ eka _ row ) ;  <NL> fill row with values ( parent obj . get kodetr ( ) . split ( split _ string ) , kod _ etr _ row ) ;  <NL>  }  COM: <s> update tables model using data from the parent object </s>