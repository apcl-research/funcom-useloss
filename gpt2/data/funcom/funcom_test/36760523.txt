TDAT: public void remove self ( ) throws io exception {  <NL>  / / todo check if transactions are active . if so abort them .  <NL> while ( this . get data dictionary ( ) . get stored relations ( ) . size ( ) > 0 ) {  <NL> iterator < stored relation > iterator = this . get data dictionary ( ) . get stored relations ( ) . values ( ) . iterator ( ) ;  <NL> stored relation stored relation = iterator . next ( ) ;  <NL> this . remove stored relation ( stored relation ) ;  <NL>  }  <NL>  }  COM: <s> this method should only be called if no transactions are active </s>