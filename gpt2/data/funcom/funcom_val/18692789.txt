TDAT: public x node parse ( string s ) {  <NL> if ( s = = null ) return null ;  <NL>  <NL>  / / remove any header <NL> if ( s . starts with ( " < ? xml " ) ) s = s . substring ( s . index of ( " ? > " ) + 2 ) . trim ( ) ;  <NL>  <NL>  / / if well - formedness check required use jdom to check it <NL> if ( _ check & & ( jdom util . string to element ( s ) = = null ) ) return null ;  <NL>  <NL> return parse ( s , 0 ) ;  <NL>  }  COM: <s> parses a simple xml string into an xnode structure </s>