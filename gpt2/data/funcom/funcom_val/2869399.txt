TDAT: public permission collection get permissions ( code source source ) {  <NL> if ( source = = null <NL> || source . equals ( system source )  <NL> || source . equals ( shell source ) )  <NL> return get all permissions ( ) ;  <NL>  <NL>  / / if we check the security desc here then keep in mind that <NL>  / / code can add properties at runtime to the resources desc !  <NL>  <NL>  / / delegate to original policy object ; required to run under web start <NL> return system policy . get permissions ( source ) ;  <NL>  }  COM: <s> return a mutable heterogeneous capable permission collection </s>