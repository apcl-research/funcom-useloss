TDAT: public void set drawing ( drawing d ) {  <NL> if ( d = = null )  <NL> d = new standard drawing ( ) ;  <NL>  <NL>  / / detach from the old one <NL> drawing current = drawing ( ) ;  <NL> if ( ( current ! = null ) & & ( current instanceof shared drawing ) )  <NL>  ( ( shared drawing ) current ) . remove drawing container change listener ( this ) ;  <NL>  <NL> super . set drawing ( d ) ;  <NL>  <NL>  / / attach to the new one <NL> if ( ( d ! = null ) & & ( d instanceof shared drawing ) )  <NL>  ( ( shared drawing ) d ) . add drawing container change listener ( this ) ;  <NL>  }  COM: <s> attach ourselves as a drawing container change listener </s>