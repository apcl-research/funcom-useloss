TDAT: public sp document get last doc for ws refresh ( ) {  <NL> sp document last doc from crawl queue = get last doc in crawl queue of action type add ( ) ;  <NL> if ( null = = last doc from crawl queue ) {  <NL> return last doc processed ;  <NL>  } else if ( last doc from crawl queue . compare to ( last doc processed ) > 0 ) {  <NL> return last doc from crawl queue ;  <NL>  } else {  <NL> return last doc processed ;  <NL>  }  <NL>  }  COM: <s> return the most suitable document to start the crawl </s>