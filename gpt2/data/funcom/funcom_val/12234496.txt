TDAT: protected sort controller get sort controller ( ) {  <NL>  / / / / this check is for the sake of the very first call after instantiation <NL>  / / doesn ' t apply for jx list ? need to test for filter enabled ?  <NL>  / / if ( filters = = null ) return null ;  <NL> if ( ! is filter enabled ( ) ) return null ;  <NL> return get filters ( ) . get sort controller ( ) ;  <NL>  }  COM: <s> returns the currently active sort controller </s>