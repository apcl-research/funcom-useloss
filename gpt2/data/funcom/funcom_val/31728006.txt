TDAT: public boolean set packet ( int t , player device id did , int size ) {  <NL> if ( ! set header ( t , did . code , did . index ) ) return false ;  <NL> header . size = size ;  <NL> buf = new byte [ player . player _ msghdr _ size + size ] ;  <NL>  / / header is buffered upon sending <NL>  / / if ( ! buffer header ( ) ) return false ;  <NL> return true ;  <NL>  }  COM: <s> readies the packet for sending but byte buffer must be filled </s>