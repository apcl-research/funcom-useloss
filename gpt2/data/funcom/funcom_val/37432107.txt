TDAT: private boolean is batchable command ( command command ) {  <NL> return ( command instanceof date command ) || <NL>  ( command instanceof find command ) || <NL>  ( command instanceof create command ) || <NL>  ( command instanceof select command ) || <NL>  ( command instanceof join command ) || <NL>  ( command instanceof load command ) || <NL>  ( command instanceof invoke command ) || <NL>  ( command instanceof start command ) || <NL>  ( command instanceof stop command ) || <NL>  ( command instanceof heimlich maneuver ) || <NL>  ( command instanceof alert command ) || <NL>  ( command instanceof status command ) ;  <NL>  }  COM: <s> internal utility method to determine if this command can </s>