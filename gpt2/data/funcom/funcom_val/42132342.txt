TDAT: private int get interval ( ) {  <NL> int interval = default _ interval ;  <NL>  <NL> if ( protocol type . equals ( protocol _ osdata ) ) {  <NL> interval = integer . parse int ( config . get ( configuration . type _ os ) ) ;  <NL>  } else if ( protocol type . equals ( protocol _ snmp ) ) {  <NL> interval = integer . parse int ( config . get ( configuration . type _ snmp ) ) ;  <NL>  } else if ( protocol type . equals ( protocol _ 1wire ) ) {  <NL> interval = integer . parse int ( config . get ( configuration . type _ 1wire ) ) ;  <NL>  } else if ( protocol type . equals ( protocol _ video ) ) {  <NL> interval = integer . parse int ( config . get ( configuration . type _ video ) ) ;  <NL>  }  <NL> return interval ;  <NL>  }  COM: <s> reads out the given configuration to determine the adapted polling </s>