TDAT: public void replace data ( object old data , object new data ) {  <NL> int row = get object row ( old data ) ;  <NL> if ( ( 0 < = row ) & & ( row < get row count ( ) ) ) {  <NL> boolean marked = is marked ( row ) ; / / remember marking <NL> unmark row ( row ) ;  <NL> rows . remove ( old data ) ;  <NL> if ( rows . contains ( new data ) )  <NL> row = get object row ( new data ) ;  <NL> else rows . add ( row , new data ) ;  <NL> if ( marked ) mark row ( row ) ;  <NL> fire table data changed ( ) ;  <NL>  }  <NL>  }  COM: <s> replaces the given old data object with the new given one and notifies </s>