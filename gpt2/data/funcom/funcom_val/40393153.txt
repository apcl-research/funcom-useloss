TDAT: public object clone ( ) {  <NL>  / / we can just use what super . clone ( ) returns : the only instance variables are <NL>  / / value objects , which are immutable , so we don ' t care that both the copy <NL>  / / and the copied refer to the same value objects . if we had mutable instance <NL>  / / variables , then of course we would need to clone them .  <NL> try { return super . clone ( ) ; }  <NL> catch ( clone not supported exception e ) { return null ; }  <NL>  }  COM: <s> returns a copy of this object </s>