TDAT: public object get synch lock for ( olat resourceable ores ) {  <NL> object synch lock ;  <NL> string key = ores . get resourceable type name ( ) + " : "  <NL>  + ores . get resourceable id ( ) ;  <NL> synchronized ( synch lock hashmap ) { / / o _ clusterok , since this class is only used by implementations of syncer ( and those provide correct singlevm / cluster code )  <NL> synch lock = synch lock hashmap . get ( key ) ;  <NL> if ( synch lock = = null ) {  <NL> synch lock = new object ( ) ;  <NL> synch lock hashmap . put ( key , synch lock ) ;  <NL>  }  <NL>  } / / end of synchronized <NL> return synch lock ;  <NL>  }  COM: <s> only to be used by implementations of syncer </s>