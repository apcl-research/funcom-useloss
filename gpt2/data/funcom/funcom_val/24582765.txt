TDAT: public string to string ( ) {  <NL> double p = 0 ;  <NL> if ( ( candidates missed + candidates hit ) > 0 )  <NL> p = ( candidates hit * 100 / ( candidates missed + candidates hit ) ) ;  <NL>  <NL> double r = 0 ;  <NL> if ( ( targets hit + targets missed ) > 0 )  <NL> r = ( ( targets hit * 100 ) / ( targets hit + targets missed ) ) ;  <NL>  <NL> return ( targets hit + targets missed ) + " "  <NL>  + ( candidates missed + candidates hit ) + " " + p + " " + r ;  <NL>  }  COM: <s> gets a string description of the current precision recall rates </s>