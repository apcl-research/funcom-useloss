TDAT: public int normalise with shift ( ) {  <NL> int len = length ( ) ;  <NL> if ( len = = math utils . shift _ koef )  <NL> return len ;  <NL> x = ( int ) ( ( ( long ) x < < math utils . shift _ count ) / len ) ;  <NL> y = ( int ) ( ( ( long ) y < < math utils . shift _ count ) / len ) ;  <NL> return len ;  <NL>  }  COM: <s> normalises this vector and shifts the result data </s>