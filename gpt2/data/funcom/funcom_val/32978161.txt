TDAT: public void connect ( service source ) {  <NL>  / / search through the servicethe ' s interfaces and try matching them <NL> for ( string name : source . interfaces . key set ( ) ) {  <NL> if ( interfaces . contains key ( name ) ) { / / there is a reference by that name <NL>  / / connect the reference to that interface <NL> interfaces . get ( name ) . connect ( source . interfaces . get ( name ) ) ;  <NL>  } else {  <NL> throw new system model exception ( this , " name mismatch trying to wire interface ' " + name + " ' of remote service ' " + source . get name ( ) + " ' " ) ;  <NL>  }  <NL>  }  <NL>  }  COM: <s> connects this service to another service </s>