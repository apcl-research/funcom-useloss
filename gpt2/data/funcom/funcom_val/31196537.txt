TDAT: private void parse line ( string line ) throws malformed url exception {  <NL> string tokenizer line tokenizer = new string tokenizer ( line , " " ) ;  <NL>  <NL> this . method = line tokenizer . next token ( ) ;  <NL> string uri string = line tokenizer . next token ( ) ;  <NL> this . uri = new url ( uri string ) ;  <NL>  <NL> if ( uri string . equals ( " http : / / - proxy . on / off - / " ) || uri string . equals ( " http : / / - proxy . on / off - " ) )  <NL> control request = true ;  <NL>  <NL> this . version = line tokenizer . next token ( ) ;  <NL>  }  COM: <s> tokenizes a code string code into three tokens and stores them in this </s>