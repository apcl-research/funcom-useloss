TDAT: public void set using shared password ( boolean is shared password ) {  <NL> object old = new boolean ( this . is shared password ) ;  <NL> this . is shared password = is shared password ;  <NL> fire property change ( " using shared password " , old ,  <NL> new boolean ( is shared password ) ) ;  <NL>  <NL>  / / this is a bad idea , since it would happen on each replica . things <NL>  / / would eventually be consistent , but it would waste cycles .  <NL>  / / if ( shared password = = null ) {  <NL>  / / set shared password ( encrypt password ( " password " ) ) ;  <NL>  / / }  <NL>  }  COM: <s> set to true if all users share a password </s>