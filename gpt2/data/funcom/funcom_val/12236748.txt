TDAT: public void set fills viewport height ( boolean fills viewport height ) {  <NL> if ( fills viewport height = = get fills viewport height ( ) ) return ;  <NL> boolean old = get fills viewport height ( ) ;  <NL> this . fills viewport height = fills viewport height ;  <NL> fire property change ( " fills viewport height " , old , get fills viewport height ( ) ) ;  <NL> revalidate ( ) ;  <NL>  }  COM: <s> sets the flag which controls the scrollable tracks viewport height property </s>